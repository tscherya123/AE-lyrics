# 🎵 AI Lyrics Aligner & After Effects Importer

Цей репозиторій містить набір інструментів для автоматичної синхронізації тексту пісень з аудіо та подальшого імпорту субтитрів у Adobe After Effects.

**Основні можливості:**

1. **AI Lyrics Aligner (`app.py`):** Використовує моделі **OpenAI Whisper (Large-v3)** та **Demucs** для ідеального вирівнювання тексту (alignment). Вміє відділяти вокал від музики для максимальної точності таймінгів.

2. **AE Importer (`LyricMaker.jsx`):** Скрипт для After Effects, який перетворює SRT-таймінги на анімовані текстові шари на основі вашого шаблону.

---

## 🛠 Встановлення (Windows)

Перед початком роботи потрібно встановити необхідні компоненти. Це робиться один раз.

### Крок 1: Встановлення Python

1. Завантажте Python (версія 3.10 або новіша) з [офіційного сайту](https://www.python.org/downloads/).
2. Під час встановлення **ОБОВʼЯЗКОВО** поставте галочку:

   ✅ **Add Python to PATH**

3. Натисніть `Install Now`.

### Крок 2: Встановлення FFmpeg

Це критично важливо для обробки аудіо. Оберіть один з варіантів:

#### Варіант 1: Автоматично (найлегший)

1. Відкрийте термінал (Win + R → `cmd` → Enter).
2. Введіть команду та погодьтесь із встановленням:

   ```cmd
   winget install "FFmpeg (Essentials)"

### Крок 2: Встановлення FFmpeg

Це критично важливо для обробки аудіо. Оберіть один з варіантів:

#### Варіант 1: Автоматично (найлегший)

1. Відкрийте термінал (Win + R → `cmd` → Enter).
2. Введіть команду та погодьтесь із встановленням:

   ```cmd
   winget install "FFmpeg (Essentials)"

   3. **Перезавантажте компʼютер.**

#### Варіант 2: Ручне встановлення (якщо Варіант 1 не спрацював)

1. **Завантаження:**
   - Перейдіть на сайт [gyan.dev/ffmpeg/builds](https://www.gyan.dev/ffmpeg/builds/).
   - У розділі “git master builds” знайдіть і скачайте файл **`ffmpeg-git-full.7z`**.

2. **Розпаковка:**
   - Розпакуйте архів.
   - Перейменуйте розпаковану папку на `ffmpeg`.
   - Перемістіть цю папку на диск C, щоб шлях виглядав так: `C:\ffmpeg`.

3. **Додавання в PATH (щоб Windows бачив програму):**
   - Натисніть **Пуск** і почніть писати: `Змінні середовища` (або `Edit the system environment variables`). Відкрийте це меню.
   - Натисніть кнопку **Environment Variables... (Змінні середовища)** знизу.
   - У верхньому вікні (“User variables”) знайдіть рядок **Path** і натисніть **Edit (Змінити)**.
   - Натисніть **New (Створити)** і вставте шлях до папки `bin`:

     ```text
     C:\ffmpeg\bin
     ```

   - Натисніть **OK** у всіх вікнах.

4. **Перевірка:**
   - Відкрийте новий термінал (`cmd`) і напишіть `ffmpeg`.
   - Якщо зʼявився текст з версією — все працює.

### Крок 3: Встановлення бібліотек

1. Відкрийте термінал у папці з проєктом (або перейдіть у неї командою `cd`).
2. Встановіть необхідні бібліотеки однією командою:

   ```cmd
   pip install customtkinter stable-ts openai-whisper demucs packaging torch

---

## 🚀 Як користуватися AI Aligner

Ця програма створює ідеальний `.srt` файл з вашої пісні та тексту.

1. **Запуск:**
   - Двічі клікніть на файл **`START.bat`**.
   - _Примітка:_ перший запуск може тривати довго (5–10 хв), оскільки програма завантажує AI-моделі (~3–4 ГБ). Наступні запуски будуть значно швидшими.

2. **Інтерфейс:**
   - Натисніть **"📂 Вибрати аудіо"** і оберіть файл пісні (`.mp3`, `.wav`, `.flac`).
   - У велике текстове поле зліва вставте текст пісні.
     - _Важливо:_ розбивайте текст на рядки так, як хочете бачити їх у субтитрах.
   - Натисніть **"СТАРТ ОБРОБКИ"**.

3. **Результат:**
   - Програма автоматично відділить вокал, розпізнає таймінги та згенерує субтитри.
   - Після завершення ви можете скопіювати SRT у буфер обміну або зберегти у файл.

---

## 🎬 Як користуватися скриптом в After Effects

Скрипт **`LyricMaker.jsx`** дозволяє імпортувати отриманий SRT-файл в After Effects, використовуючи ваш власний стиль тексту.

### Встановлення скрипта

Є два варіанти запуску:

- **Разовий:** в After Effects натисніть `File` → `Scripts` → `Run Script File...` і оберіть `LyricMaker.jsx`.
- **Постійний:** скопіюйте файл `LyricMaker.jsx` у папку:

  ```text
  C:\Program Files\Adobe\Adobe After Effects [Version]\Support Files\Scripts\ScriptUI Panels
Після цього панель зʼявиться в меню `Window`.

### Процес роботи

1. Створіть у композиції **один текстовий шар**.
2. Налаштуйте його ідеально: оберіть шрифт, розмір, колір, тіні, вирівнювання та позицію на екрані. Це буде ваш **шаблон (Template)**.
3. Переконайтеся, що цей шар **виділено**.
4. Запустіть скрипт.
5. Вставте вміст вашого `.srt` файлу у вікно скрипта.
6. Оберіть режим генерації:
   - **Один шар (Source Text):** створить один шар, де текст змінюється за допомогою ключових кадрів (зручно для чистоти таймлайну).
   - **Окремі шари:** створить окремий шар для кожного рядка (зручно, якщо треба вручну рухати окремі фрази).
7. Натисніть **"Створити субтитри"**.

Скрипт створить субтитри на основі вашого дизайну, а оригінальний шар-шаблон сховає.

---

## ❓ Можливі проблеми

- **Помилка `FFmpeg not found`:** ви не встановили FFmpeg, не додали його в PATH або не перезавантажили компʼютер.
- **Програма довго “висить” при натисканні `Старт`:** це нормально для першого запуску — йде завантаження моделей Whisper Large-v3 та Demucs. Слідкуйте за логами в консолі.

---

**Author:** 057FETTY